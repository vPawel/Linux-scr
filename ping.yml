---
  - name: "Playing with Ansible"
    hosts: localhost
    connection: local
    tasks:

    - name: "just execute a ls command"
      shell: "ls -la /"
      register: "output"

    - debug: var=output.stdout_lines